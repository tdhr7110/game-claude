# CHIMERA BUTCHER — Agent Workflow

このリポジトリでClaude Code、Codex、その他のAIエージェントが作業するときの共通ルール。

## 作業開始時

1. `00_PROJECT_HUB.md` を読み、現在地・最新TEST・次の3件を確認する。
2. `DECISIONS.md` を読み、確定済みの仕様と未決事項を区別する。
3. TESTの作成・統合・本番反映に関係する場合は `TEST_HISTORY.md` を読む。
4. ドキュメントとコードが食い違う場合は、コード・Git履歴・ユーザーの最新指示を確認し、推測で決めない。

## 作業中

- 1タスク1成果を基本とし、依頼と無関係な変更を混ぜない。
- 本番は安定版として扱い、新機能は原則TESTブランチで検証する。
- 本番URL、既存TEST URL、保存データを不用意に壊さない。
- ユーザーが明示していない大規模統合、削除、公開設定変更は行わない。
- UIはスマホ縦画面を基本とする。
- オートバトルを維持し、コマンドによる判断を加える。
- 通常能力は `Trigger + Effect`、特殊能力は `Custom Handler` を基本とし、能力連鎖には無限ループ対策を入れる。
- 部位・コマンド・シナジー・Effectの構造を変更した場合は、Excel出力処理の列定義・説明マッピングも確認する。

## 作業終了時の半自動ドキュメント更新

実装と検証が終わったら、次の判定を行い、**必要なファイルだけ**更新する。内容に実質的な変更がなければ更新しない。

### `00_PROJECT_HUB.md` を更新する条件

- 現在の本番・最新TEST・統合状態が変わった。
- 次にやる3件のいずれかが完了、変更、追加された。
- 重要な未解決事項、URL、公開状態が変わった。

更新時は「次にやる3件」を必ず3件以内に保つ。全バックログを転記しない。

### `DECISIONS.md` を更新する条件

- ユーザーが仕様・方針を確定、変更、撤回した。
- 実装により、暫定仕様を正式採用または廃止する判断が確定した。

提案や推測を `[確定]` にしない。判断待ちは `[未決]`、検証中は `[暫定]` として記録する。

### `TEST_HISTORY.md` を更新する条件

- 新しいTESTブランチ・公開URLを作成した。
- TEST間の統合、本番反映、廃止を行った。
- TESTの目的や主要機能が変わった。
- 回帰テストや重要な修正を追加した。

可能ならブランチ名、日付、コミット、公開URL、統合状態を記録する。

## 検証

コード変更時は、変更範囲に応じて次を実行する。

- `npm run build`
- `npm run lint`
- 関連する自動テストまたはスクリプト
- 主要フローの手動確認
- ブラウザコンソールの重大エラー確認

## 最終報告

作業終了時は次を簡潔に報告する。

1. 変更した内容
2. 実行した検証と結果
3. 更新した管理Markdown（更新不要ならその旨）
4. 残る未解決事項
