# CHIMERA BUTCHER — DECISIONS

> 最終更新: 2026-08-19（JST）  
> 確定した方針を、後から迷わないために残す。  
> `[確定]` は今後の前提、`[暫定]` はテストで変更可能、`[未決]` は判断待ち。

## 1. ゲームの核

- `[確定]` 1ランは実プレイで5〜10分を目標とする。
- `[確定]` 戦闘の基本はオートバトルを維持する。
- `[確定]` コマンド要素を拡大し、プレイヤーの判断と介入を増やす。
- `[確定]` 倒した敵から部位を獲得し、自分のキメラへ装着する。
- `[確定]` 固定装備枠は設けず、同じ種類の部位を複数装着できる。右腕6本なども成立させる。
- `[確定]` 図鑑・ビルド収集と「ぶっ壊れビルド完成時の快感」を重視する。
- `[確定]` UIはシンプルにし、部位能力・シナジー・コンボは複雑でよい。
- `[確定]` Commonを単なる下位レアにせず、低コスト・シナジー素材・大量装着で価値を持たせる。

## 2. 戦闘・報酬導線

- `[確定]` 敵撃破後は、部位獲得だけでなくコマンド獲得・入れ替え・進化も目立つ導線で扱う。
- `[確定]` 部位選択後、可能な限り画面遷移を増やさずコマンド変更・進化へ進める。
- `[確定]` 後から変更する場合は、戦闘画面下部の「コマンド」導線から行う。
- `[確定]` 部位獲得、コマンド獲得、進化は演出を強化し、レア度に応じて派手さを変える。
- `[確定]` 敵が実際に持つ部位を設定し、その構成部位をドロップ候補にする。
- `[確定]` 敵候補を見て、狙える部位や危険なギミックをある程度判断できるようにする。
- `[確定]` 特定ビルドを完全無効化する敵は避ける。
- `[暫定]` 手動操作量は全体の20〜30%程度を目安とする。

## 3. UI・ビジュアル

- `[確定]` ブラウザでの基本レイアウトはスマホ縦画面を前提とする。
- `[確定]` 重要情報だけを常時表示し、詳細はツールチップ・図鑑・詳細画面に分ける。
- `[確定]` 世界観は「ダークファンタジー × 異形生物 × 少しコミカル」。
- `[確定]` 画像・演出はゲーム性を確認しながら段階的に強化する。
- `[確定]` BGMより先にSEを優先する。
- `[暫定]` プレイヤー本体はコア＋部位レイヤー方式を中心に検討する。

### 旧方針の訂正

2026-08-18版TODOには「横画面前提」とあるが、その後の判断で**縦画面前提へ変更済み**。今後は縦画面を正とする。

## 4. 能力・データ設計

- `[確定]` 通常能力は `Trigger + Effect` 方式で管理する。
- `[確定]` 1能力に複数Effectと条件を設定できるようにする。
- `[確定]` 特殊能力は `Custom Handler` 方式で管理する。
- `[確定]` 完全に新しいゲームルールだけ、Claude/Codexにコード追加させる。
- `[確定]` 能力連鎖には無限ループ防止処理を入れる。
- `[確定]` 部位・コマンド・シナジーのExcelは、ゲームのソースデータから自動生成し二重管理しない。
- `[確定]` データ型やEffectを追加・変更したときは、`scripts/exportGameData.ts` の列定義・説明マッピングも更新する。
- `[確定]` 管理画面から、可能な限り数値・部位・能力をユーザー自身で調整できるようにする。

## 5. セーブ・図鑑

- `[確定]` 既存のキメラ図鑑は削除しない。
- `[確定]` TESTごとに保存領域を分け、別TESTのデータを壊さない。
- `[確定]` 保存データには `saveVersion` を持たせる。
- `[暫定]` TEST10ではラン途中保存・再開、部位図鑑、敵図鑑、キメラ収集図鑑を検証する。
- `[未決]` TEST10の保存仕様をそのまま本番へ採用するか。

## 6. TEST・本番運用

- `[確定]` 本番は安定版として扱う。
- `[確定]` 新機能は独立したTESTブランチとURLで検証する。
- `[確定]` 面白かった機能だけを本番へ反映する。
- `[確定]` 既存の本番・TEST URLを不用意に壊さない。
- `[確定]` 複数ブランチを統合するときは、先に競合・問題点・統合順序を確認する。
- `[確定]` 大きな統合は専用ブランチで行い、ビルド・lint・回帰確認後に本番反映を判断する。
- `[未決]` TEST10を丸ごと本番へ統合するか、機能単位で選別するか。

## 7. 公開・コード保護

- `[確定]` 公開しても、ブラウザへ配信したコードを完全に秘匿することはできない。
- `[方針]` 将来はPrivateな開発リポジトリからCloudflare Pages、Vercel、Netlify等へビルド・公開する構成を優先する。
- `[確定]` リポジトリ移行前にZIPバックアップ、秘密情報・個人情報・会社情報・`.env`の確認を行う。
- `[確定]` 新環境の動作確認が完了するまで、旧リポジトリを削除しない。
- `[未決]` 現在のPublicリポジトリをPrivate化する具体的な日程と公開経路。

## 8. 現時点で優先しないもの

- オンライン対戦
- ランキング
- アカウント機能
- 3D化
- 完全なSlay the Spire型カードデッキ

## 9. プレイデータ収集

- `[現状]` 現在のコードは基本的にブラウザ内で完結し、全プレイヤーの勝率等を集約する仕組みは確認できない。
- `[未決]` 難易度調整用の匿名プレイデータを収集するか。
- `[未決]` 収集する場合の項目、同意表示、匿名化、保存期間、送信先。

## 10. 情報源

- [リポジトリREADME](https://github.com/tdhr7110/game-claude/blob/claude/chimera-battle-prototype-36bome/README.md)
- [TEST10ブランチ](https://github.com/tdhr7110/game-claude/tree/test10/chimera-integrated-next)
- [デプロイ設定](https://github.com/tdhr7110/game-claude/blob/claude/chimera-battle-prototype-36bome/.github/workflows/deploy-pages.yml)
- `CHIMERA BUTCHER 開発TODO.md`（2026-08-18版）
- プロジェクト内の決定・会話（2026-08-16〜19）

